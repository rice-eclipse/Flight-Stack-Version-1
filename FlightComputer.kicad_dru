(version 1)
(rule "class 0:0"
  (condition "A.NetClass == 'Default' && B.NetClass == 'Default'")
  (constraint clearance (min 0.15mm)))

(rule "class 1:1"
  (condition "A.NetClass == 'USB_DIFF_90' && B.NetClass == 'USB_DIFF_90'")
  (constraint clearance (min 0.15mm)))
